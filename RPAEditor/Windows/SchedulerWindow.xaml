<Window x:Class="RPAEditor.Windows.SchedulerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RPAEditor.Windows"
        Title="スケジューラ" Height="600" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="White">
    <Window.Resources>
        <local:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ツールバー -->
        <Border Grid.Row="0" Background="#F5F5F5" BorderBrush="#DDDDDD" BorderThickness="0,0,0,1" Padding="10">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="btnAddTask" Content="➕ タスク追加" Width="120" Height="30" Margin="0,0,10,0"/>
                <Button x:Name="btnEditTask" Content="✏ 編集" Width="80" Height="30" Margin="0,0,10,0"/>
                <Button x:Name="btnDeleteTask" Content="🗑 削除" Width="80" Height="30" Margin="0,0,10,0"/>
                <Separator Width="20" Background="Transparent"/>
                <Button x:Name="btnStartScheduler" Content="▶ 開始" Width="80" Height="30" Margin="0,0,10,0"/>
                <Button x:Name="btnStopScheduler" Content="⏹ 停止" Width="80" Height="30" Margin="0,0,10,0"/>
                <Separator Width="20" Background="Transparent"/>
                <TextBlock x:Name="txtSchedulerStatus" Text="停止中" VerticalAlignment="Center" FontWeight="Bold" Foreground="#999999"/>
            </StackPanel>
        </Border>

        <!-- メインコンテンツ（2列） -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左：タスクリスト -->
            <Border Grid.Column="0" BorderBrush="#DDDDDD" BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="スケジュールタスク" FontSize="16" FontWeight="SemiBold" Margin="15,15,15,10"/>

                    <ListBox x:Name="lstTasks" Grid.Row="1" BorderThickness="0" Margin="10">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#EEEEEE" BorderThickness="0,0,0,1" Padding="10" Background="Transparent">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- 有効/無効アイコン -->
                                        <TextBlock Grid.Column="0" Text="{Binding EnabledIcon, Mode=OneWay}" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>

                                        <!-- タスク情報 -->
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding Name, Mode=OneWay}" FontSize="14" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding ScheduleDescription, Mode=OneWay}" FontSize="12" Foreground="#666666" Margin="0,2,0,0"/>
                                            <TextBlock FontSize="11" Foreground="#999999" Margin="0,2,0,0">
                                                <Run Text="次回: "/>
                                                <Run Text="{Binding NextRunText, Mode=OneWay}"/>
                                            </TextBlock>
                                        </StackPanel>

                                        <!-- 状態アイコン -->
                                        <TextBlock Grid.Column="2" Text="{Binding StatusIcon, Mode=OneWay}" FontSize="16" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="#DDDDDD"/>

            <!-- 右：実行履歴 -->
            <Border Grid.Column="2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="実行履歴" FontSize="16" FontWeight="SemiBold" Margin="15,15,15,10"/>

                    <ListBox x:Name="lstHistory" Grid.Row="1" BorderThickness="0" Margin="10">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#EEEEEE" BorderThickness="0,0,0,1" Padding="10" Background="Transparent">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- ステータスアイコン -->
                                        <TextBlock Grid.Column="0" Text="{Binding StatusIcon, Mode=OneWay}" FontSize="16" VerticalAlignment="Top" Margin="0,0,10,0"/>

                                        <!-- 履歴情報 -->
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding TaskName, Mode=OneWay}" FontSize="13" FontWeight="SemiBold"/>
                                            <TextBlock FontSize="11" Foreground="#666666" Margin="0,2,0,0">
                                                <Run Text="{Binding StartTime, StringFormat=yyyy/MM/dd HH:mm:ss, Mode=OneWay}"/>
                                                <Run Text=" - "/>
                                                <Run Text="{Binding DurationText, Mode=OneWay}"/>
                                            </TextBlock>
                                            <TextBlock Text="{Binding ErrorMessage}" FontSize="11" Foreground="#CC0000" Margin="0,2,0,0" TextWrapping="Wrap"
                                                      Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>

        <!-- ステータスバー -->
        <Border Grid.Row="2" Background="#F5F5F5" BorderBrush="#DDDDDD" BorderThickness="0,1,0,0" Padding="10">
            <Grid>
                <TextBlock x:Name="txtMemoryUsage" Text="メモリ使用量: 計測中..." FontSize="12" Foreground="#666666"/>
            </Grid>
        </Border>
    </Grid>
</Window>
