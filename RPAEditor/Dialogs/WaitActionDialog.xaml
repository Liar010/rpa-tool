<Window x:Class="RPAEditor.WaitActionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="待機設定" Height="230" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="待機時間（ミリ秒）:" Margin="0,5"/>
        <TextBox x:Name="txtMilliseconds" Grid.Row="1" Margin="0,5" Text="1000"/>

        <TextBlock Grid.Row="2" Text="※ 1000ミリ秒 = 1秒"
                   FontStyle="Italic"
                   Foreground="Gray"
                   Margin="0,10"
                   VerticalAlignment="Top"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="btnOK" Content="OK" Width="80" Height="30" Margin="5" IsDefault="True"/>
            <Button x:Name="btnCancel" Content="キャンセル" Width="80" Height="30" Margin="5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
