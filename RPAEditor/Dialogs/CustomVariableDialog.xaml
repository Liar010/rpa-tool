<Window x:Class="RPAEditor.Dialogs.CustomVariableDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="カスタム変数" Height="250" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 説明 -->
        <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,15" Foreground="#666666">
            カスタム変数を定義すると、ファイル保存時に %変数名% で使用できます
        </TextBlock>

        <!-- 変数名 -->
        <TextBlock Grid.Row="1" Text="変数名（%は不要）:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="2" x:Name="txtVariableName"
                 Height="30" FontSize="14" FontFamily="Consolas"
                 VerticalContentAlignment="Center"
                 Margin="0,0,0,15"/>

        <!-- 値 -->
        <TextBlock Grid.Row="3" Text="値:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="4" x:Name="txtVariableValue"
                 Height="30" FontSize="14" FontFamily="Consolas"
                 VerticalContentAlignment="Center"
                 Margin="0,0,0,15"/>

        <!-- ボタン -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnOK" Content="OK" Width="80" Height="30" Margin="0,0,10,0" IsDefault="True"/>
            <Button x:Name="btnCancel" Content="キャンセル" Width="80" Height="30" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
