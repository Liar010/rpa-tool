<Window x:Class="RPAEditor.Dialogs.ExcelOpenDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Excel: ファイルを開く" Height="250" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ファイルパス -->
        <TextBlock Grid.Row="0" Text="Excelファイルパス:" Margin="0,0,0,5"/>
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="txtFilePath" Grid.Column="0" Margin="0,0,5,0" VerticalContentAlignment="Center"/>
            <Button x:Name="btnBrowse" Grid.Column="1" Content="参照..." Width="80" Click="BtnBrowse_Click"/>
        </Grid>

        <!-- オプション -->
        <TextBlock Grid.Row="2" Text="オプション:" Margin="0,0,0,5"/>
        <CheckBox x:Name="chkCreateIfNotExists" Grid.Row="3" Content="ファイルが存在しない場合は新規作成する"
                  VerticalAlignment="Top" Margin="0,5,0,0"/>

        <!-- ボタン -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="OK" Width="100" Height="32" Click="BtnOk_Click" IsDefault="True" Margin="0,0,10,0"/>
            <Button Content="キャンセル" Width="100" Height="32" Click="BtnCancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
