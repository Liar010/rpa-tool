<Window x:Class="RPAEditor.Dialogs.LoopStartActionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ループ開始アクション"
        Height="200" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- コメント -->
        <TextBlock Grid.Row="0" Text="コメント（任意）:" Margin="0,0,0,5"/>
        <TextBox x:Name="txtComment" Grid.Row="0" Margin="0,20,0,0" Height="25"/>

        <!-- 説明 -->
        <TextBlock Grid.Row="1" Margin="0,15,0,0" TextWrapping="Wrap" Foreground="#666666">
            <Run Text="• ループの開始位置を示すマーカーです"/>
            <LineBreak/>
            <Run Text="• 対応するループ終了アクションと組み合わせて使用します"/>
        </TextBlock>

        <!-- ボタン -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="btnOK" Content="OK" Width="80" Height="30" Margin="0,0,10,0" IsDefault="True"/>
            <Button x:Name="btnCancel" Content="キャンセル" Width="80" Height="30" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
