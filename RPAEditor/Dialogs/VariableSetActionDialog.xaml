<Window x:Class="RPAEditor.Dialogs.VariableSetActionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="変数設定アクション"
        Height="280" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 操作種類 -->
        <TextBlock Grid.Row="0" Text="操作種類:" Margin="0,0,0,5"/>
        <ComboBox x:Name="cmbOperationType" Grid.Row="0" Margin="0,20,0,0" Height="25">
            <ComboBoxItem Content="設定 (=)" Tag="Set" IsSelected="True"/>
            <ComboBoxItem Content="加算 (+=)" Tag="Add"/>
            <ComboBoxItem Content="減算 (-=)" Tag="Subtract"/>
        </ComboBox>

        <!-- 変数名 -->
        <TextBlock Grid.Row="1" Text="変数名:" Margin="0,15,0,5"/>
        <TextBox x:Name="txtVariableName" Grid.Row="1" Margin="0,35,0,0" Height="25"/>

        <!-- 値 -->
        <TextBlock Grid.Row="2" Text="値:" Margin="0,15,0,5"/>
        <TextBox x:Name="txtValue" Grid.Row="2" Margin="0,35,0,0" Height="25"/>

        <!-- 説明 -->
        <TextBlock Grid.Row="3" Margin="0,15,0,0" TextWrapping="Wrap" Foreground="#666666">
            <Run Text="• 変数名は半角英数字とアンダースコアのみ使用可能"/>
            <LineBreak/>
            <Run Text="• 設定: 変数に値を代入します"/>
            <LineBreak/>
            <Run Text="• 加算/減算: 数値の変数に対して演算を行います"/>
        </TextBlock>

        <!-- ボタン -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="btnOK" Content="OK" Width="80" Height="30" Margin="0,0,10,0" IsDefault="True"/>
            <Button x:Name="btnCancel" Content="キャンセル" Width="80" Height="30" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
